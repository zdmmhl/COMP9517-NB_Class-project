# 模型输出格式指南

这份文档用于约定训练模块和评估模块之间的交接格式。负责训练模型的同学按照这里的目录和文件格式保存结果，评估模块就可以在不了解模型内部实现的情况下，统一计算指标和生成图表。

## 1. 基本原则

所有方法必须：

- 使用同一份类别列表和 `class_mapping.csv`。
- 使用同一份 train、validation 和 test 划分。
- 只在最终确定的 test set 上生成测试预测。
- 使用相同的 `sample_id` 和类别编号。
- 图片路径使用相对于数据集根目录的相对路径。
- 不把数据集、模型权重和实验输出提交到 GitHub。

评估代码不能写死为只支持两个模型。建议至少支持以下三种方法：

```text
hog_svm
cnn_scratch
resnet18_pretrained
```

如果后续更换模型，只需使用新的方法名称，并继续遵守相同的输出格式。

## 2. 推荐保存目录

本项目固定使用500类数据划分，正式 run ID 为 `inat500_seed9517`。
这里约定的是主500类基准中训练模块与评价模块的交接格式。类别规模Advanced
实验使用专用的 `configs/class_scaling.json` 和
`outputs/final_results/inat500/advanced/class_scaling/`，不会把1000类或
2500类预测混入主500类比较目录。

按照当前仓库结构保存：

```text
COMP9517-NB_Class-project/
|-- checkpoints/
|   |-- hog_svm/
|   |-- cnn_scratch/
|   `-- resnet18_pretrained/
|
|-- logs/
|   |-- hog_svm/
|   |   `-- training_metadata.json
|   |-- cnn_scratch/
|   |   `-- history.csv
|   `-- resnet18_pretrained/
|       `-- history.csv
|
`-- outputs/
    `-- runs/
        `-- <run_id>/
            |-- run_manifest.json
            `-- predictions/
                |-- hog_svm/
                |   |-- predictions.csv
                |   `-- metadata.json
                |-- cnn_scratch/
                |   |-- predictions.csv
                |   `-- metadata.json
                `-- resnet18_pretrained/
                    |-- predictions.csv
                    `-- metadata.json
```

目录职责：

- `checkpoints/`：保存训练后的模型权重或传统分类器文件。
- `logs/`：保存训练过程和每个 epoch 的指标。
- `outputs/runs/<run_id>/predictions/`：保存该数据划分下最终 test set 的预测结果，交给评估模块读取。
- `outputs/runs/<run_id>/run_manifest.json`：记录类别数量、split seed、类别文件和测试集身份。

## 3. 共同使用的数据文件

所有方法共同使用以下文件：

```text
data_splits/
|-- selected_classes.csv
|-- class_mapping.csv
|-- train.csv
|-- validation.csv
`-- test.csv
```

`class_mapping.csv` 推荐格式：

```csv
class_index,category_id,species_name
0,12345,Species A
1,67890,Species B
```

其中：

- `class_index` 是模型实际使用的连续类别编号，即 `0-499`。
- `category_id` 是 iNaturalist 原始类别编号。
- `species_name` 用于图表和错误分析。

所有模型的 `true_label` 和预测标签必须使用同一套 `class_index`。

## 4. predictions.csv

每个模型必须生成一份：

```text
outputs/runs/<run_id>/predictions/<method_name>/predictions.csv
```

推荐字段：

```csv
sample_id,image_path,true_label,pred_1,pred_2,pred_3,pred_4,pred_5,score_1,score_2,score_3,score_4,score_5
10001,val/class_a/image1.jpg,42,42,17,96,8,51,9.2,7.8,6.4,5.9,5.1
10002,val/class_b/image2.jpg,17,96,17,42,51,8,8.7,8.1,7.2,6.3,5.4
```

字段说明：

- `sample_id`：test manifest 中的唯一图片编号。
- `image_path`：相对于 `data.root` 的图片路径。
- `true_label`：真实类别的 `class_index`。
- `pred_1`：分数最高的预测类别。
- `pred_2` 到 `pred_5`：其余 Top-5 预测类别。
- `score_1` 到 `score_5`：对应的模型输出分数。

必须满足：

- `sample_id` 不能重复。
- `pred_1` 到 `pred_5` 按分数从高到低排列。
- `true_label` 和预测标签必须处于合法类别范围内。
- 每个测试样本恰好出现一次。
- 固定500类、每类10张test图片，因此文件必须有5,000行数据，不包括表头。

对于 CNN，`score` 可以是 logit 或 probability。对于 Linear SVM，`score` 通常是 `decision_function` 的输出。SVM decision score 和 CNN logit/probability 不能直接跨模型比较大小。

## 5. metadata.json

每个模型必须生成：

```text
outputs/runs/<run_id>/predictions/<method_name>/metadata.json
```

推荐格式：

```json
{
  "method_name": "resnet18_pretrained",
  "model_type": "resnet18",
  "initialization": "imagenet_pretrained",
  "num_classes": 500,
  "random_seed": 9517,
  "split_id": "inat500_seed9517",
  "score_type": "logit",
  "training_time_seconds": 3600.0,
  "inference_time_seconds": 48.0,
  "inference_num_images": 5000,
  "inference_includes_preprocessing": true,
  "device": "NVIDIA T4",
  "batch_size": 64,
  "image_size": 224
}
```

不同方法的 `split_id`、`num_classes` 和 `random_seed` 应保持一致。

`score_type` 推荐值：

```text
hog_svm              -> decision_function
cnn_scratch          -> logit 或 probability
resnet18_pretrained  -> logit 或 probability
```

时间测量必须写清楚是否包含图片读取和预处理。传统方法如果需要单独记录 HOG 特征提取时间，可以额外添加：

```json
{
  "feature_extraction_time_seconds": 600.0,
  "classifier_training_time_seconds": 300.0
}
```

## 6. history.csv

深度学习模型应保存：

```text
logs/<method_name>/history.csv
```

推荐格式：

```csv
epoch,train_loss,val_loss,train_top1,val_top1,val_top5,val_macro_f1,learning_rate,epoch_seconds
1,6.42,6.31,0.03,0.04,0.08,0.02,0.001,65.4
2,5.87,5.62,0.08,0.10,0.18,0.07,0.001,64.8
```

这份文件用于生成：

- Train/validation loss 曲线。
- Train/validation accuracy 曲线。
- Validation Top-5 曲线。
- Validation Macro-F1 曲线。
- 每个 epoch 的运行时间。

传统 HOG + SVM 没有 epoch，因此不需要生成相同的 `history.csv`，但必须在 metadata 中记录特征提取、训练和推理时间。

## 7. 可选的完整分数

如果实现方便，可以额外保存：

```text
outputs/runs/<run_id>/predictions/<method_name>/full_scores.npz
```

内容可以包括：

```text
sample_ids: shape [N]
y_true:     shape [N]
scores:     shape [N, num_classes]
```

对于 8,000 个测试样本和 800 个类别，未压缩的 float32 分数矩阵约为 25.6 MB。它适合更深入的置信度分析，但不是计算项目强制指标的必要文件，也不应上传到 GitHub或放入最终 code ZIP。

## 8. 交付前检查

训练负责人提交预测结果前，应检查：

- 预测文件是否来自最终 test set。
- 是否恰好包含 test manifest 中的所有样本。
- 是否存在重复或缺失的 `sample_id`。
- 是否使用正确的 `class_mapping.csv`。
- Top-5 是否按分数降序排列。
- 不同模型的 `true_label` 是否完全一致。
- Metadata 是否包含训练和推理时间。
- 深度学习是否保存完整的训练历史。
- 文件中是否没有个人电脑的绝对路径。

建议在完整训练前先输出一份小规模测试文件，让评估模块提前验证格式。
