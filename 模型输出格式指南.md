# 模型输出格式指南

这份文档记录当前仓库实际使用的训练结果格式。深度模型和传统模型的原始输出
不同，正式评估流程通过各自的导出器整理结果，不要求训练代码额外转换成另一套
统一 CSV。

## 1. 基本原则

- 主实验固定使用 `inat500_seed9517` 数据划分。
- 所有方法必须使用相同的类别编号和 train/validation/test 清单。
- 测试图片只用于最终评估，不参与模型或超参数选择。
- 图片路径使用相对于数据集根目录的路径。
- 本地 `results/`、checkpoint、数据集和特征缓存不提交到 Git。
- 用于组内共享和报告的精简结果保存在
  `outputs/final_results/inat500/`。

类别规模 Advanced 实验使用 `configs/class_scaling.json` 和独立的
`outputs/final_results/inat500/advanced/class_scaling/`，不会混入主500类
模型排名。

## 2. 深度模型原始输出

`training/train_deep.py` 和 `scripts/train_deep.py` 将一次运行保存在：

```text
results/<run_name>/
|-- metrics.json
|-- history.json
|-- history.csv
|-- training_state.json
|-- test_predictions.csv
|-- test_classification_report.json
|-- training_curves.png
|-- learning_rate.png
|-- test_confusion_matrix.png
|-- test_confusion_matrix_full.png
|-- test_prediction_examples.png
|-- best_model.pt
`-- last_checkpoint.pt
```

checkpoint 只保存在训练机器或课程允许的外部存储中，不进入普通 Git 提交。

### test_predictions.csv

当前深度训练代码由 `utils/serialization.py::save_predictions` 写入以下字段：

```csv
file_name,true_class_index,pred_class_index,correct,pred_1,pred_2,pred_3,pred_4,pred_5,score_1,score_2,score_3,score_4,score_5,top5_correct
```

- `file_name`：相对于数据集根目录的图片路径。
- `true_class_index`：真实连续类别编号。
- `pred_class_index`：Top-1 预测类别。
- `correct`：Top-1 是否正确。
- `pred_1` 至 `pred_5`：按分数降序排列的 Top-5 类别。
- `score_1` 至 `score_5`：对应 softmax 概率。
- `top5_correct`：真实类别是否位于 Top-5。

固定500类主测试集应有5,000行数据，不包括表头。每行的五个预测类别必须互不
重复，并按对应分数从高到低排列。

### metrics.json 和 history.csv

`metrics.json` 保存模型名称、初始化方式、设备、参数、Top-1、Top-5、
Macro Precision/Recall/F1、测试损失、训练时间和推理时间。没有在原实验中记录
的时间保持为空，不进行估算。

`history.csv` 保存每个 epoch 的训练与验证 loss、Top-1、Top-5、Macro-F1、
学习率、耗时、吞吐量和峰值显存，用于生成收敛曲线。

## 3. 传统模型原始输出

`traditional/experiment.py` 和 `scripts/run_traditional_experiment.py` 保存：

```text
results/<traditional_run>/
|-- metrics.json
|-- train_predictions.csv
|-- val_predictions.csv
|-- test_predictions.csv
|-- train_predictions_top5.csv
|-- val_predictions_top5.csv
|-- test_predictions_top5.csv
|-- test_classification_report.json
|-- test_confusion_matrix.npy
`-- model.joblib
```

传统模型的 Top-1 文件字段为：

```csv
file_name,true_class_index,pred_class_index,correct
```

Top-5 文件字段为：

```csv
file_name,true_class_index,top5_correct,top1_class_index,top1_score,top2_class_index,top2_score,top3_class_index,top3_score,top4_class_index,top4_score,top5_class_index,top5_score
```

传统分类器的 score 通常来自 `decision_function` 或类别概率，不能和深度模型的
softmax 概率直接比较数值大小。

## 4. 正式共享结果

原始运行经过导出后保存在：

```text
outputs/final_results/inat500/
|-- methods/<method_key>/
|-- comparison/
|-- ablations/
|-- advanced/class_scaling/
`-- reproducibility/
```

每个 `methods/<method_key>/` 至少包含：

```text
metrics.csv
configuration.csv
per_class_metrics.csv
test_predictions_top1.csv
confusion_matrix.csv
confusion_matrix_full.png
confusion_matrix_subset.png
top_confusions.csv
```

有逐图片 Top-5 记录的方法还包含：

```text
test_predictions_top5.csv
top5_prediction_validation.json
top1_wrong_top5_correct.csv
top1_wrong_top5_correct.png
```

不同模型的原始 Top-5 字段可以保持上述深度或传统格式。评估和导出代码按方法
读取这些格式，并统一生成 `metrics.csv`、`per_class_metrics.csv` 和 comparison
图表。不要为了表面统一而手工改动已经验证过的预测文件。

## 5. 当前导出入口

```powershell
# 从本地完整原始运行重新导出主结果
python scripts/export_final_results.py

# 仅使用Git中已有的正式CSV刷新全部报告图表
python scripts/refresh_final_comparison.py

# 导出深度消融和类别规模Advanced结果
python scripts/export_deep_ablations.py
python scripts/export_class_scaling.py
python scripts/write_experiment_docs.py
```

`refresh_final_comparison.py` 不读取 checkpoint、数据集或原始图片，适合在
逐图片预测或指标更新后重新同步：

```text
comparison/summary_metrics.csv
comparison/traditional_summary_metrics.csv
comparison/runtime_comparison.csv
comparison/model_comparison.png
comparison/runtime_vs_performance.png
comparison/per_class_f1_distribution.png
comparison/traditional_methods_comparison.png
ablations/deep_learning/*/ablation_metrics.png
ablations/deep_learning/*/ablation_deltas.png
ablations/deep_learning/*/training_curves.png
ablations/**/test_confusion_matrix.png
advanced/class_scaling/class_scaling_metrics.png
advanced/class_scaling/sample_size_control.png
advanced/class_scaling/runtime_comparison.png
advanced/class_scaling/runs/**/test_confusion_matrix.png
artifact_manifest.csv
```

紧凑混淆矩阵由对应目录当前的 `test_predictions_top1.csv` 和 `metrics.csv`
重新计算，不复制旧运行目录中的图片。

## 6. 可选的通用评估接口

`scripts/evaluate_artifacts.py` 仍支持一个独立的标准化输入格式：

```csv
sample_id,image_path,true_label,pred_1,pred_2,pred_3,pred_4,pred_5,score_1,score_2,score_3,score_4,score_5
```

它用于接入仓库外部的新模型结果，示例位于
`tests/fixtures/mock_small/`。当前正式训练流程不依赖这套可选格式，也不要求
已有深度或传统结果转换成它。

## 7. 交付前检查

- 测试预测是否来自固定 test manifest。
- 主500类预测是否恰好包含5,000张图片。
- 是否存在重复或缺失图片。
- 类别编号是否与当前 `class_mapping.json` 一致。
- Top-5 是否按分数降序排列。
- 汇总 Top-1、Top-5 和 Macro-F1 是否能从逐图片记录重新计算。
- 训练历史和实际记录到的运行时间是否完整保存。
- 缺失的历史时间是否保持空值并通过 `timing_note` 说明。
- 文件中是否没有个人电脑的绝对路径。
- checkpoint、数据集、特征缓存和完整概率矩阵是否排除在代码 ZIP 之外。
