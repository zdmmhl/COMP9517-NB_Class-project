# Demo

The final software demonstration will load a selected model, accept an image,
and display the predicted species and Top-5 scores. Implementation is deferred
until the classification pipeline is stable.
